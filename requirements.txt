# Development superset — everything production needs plus local-only tooling.
# Install with: pip install -r requirements.txt

-r api/requirements.txt

# Hyperparameter search (enhanced_predictor / Optuna experiments)
optuna>=3.5.0

# Excel export
openpyxl>=3.1.0

# Testing
pytest>=8.0.0
