# Production dependencies — this is the ONLY file Railway installs
# (see railway.json buildCommand), so it must be self-sufficient.

# API layer
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.5.0,<3.0
sse-starlette>=1.8.0
slowapi>=0.1.9
python-multipart>=0.0.9

# Database / storage
psycopg2-binary>=2.9.9
supabase>=2.0.0

# Config
python-dotenv>=1.0.0

# Data / scientific core (imported by nba_evaluator, db, game_predictor)
pandas>=2.0.0,<3.0
numpy>=1.26.0,<3.0
scipy>=1.10.0
python-dateutil>=2.8.0

# Machine learning — scikit-learn pinned to the 1.6.x series because player
# model pickles were trained under 1.6.1 and do not load across minor versions
scikit-learn>=1.6.1,<1.7
joblib>=1.3.0
xgboost>=2.1.0,<3.0
lightgbm>=4.5.0,<5.0

# NBA data sources
nba_api>=1.4.1
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
